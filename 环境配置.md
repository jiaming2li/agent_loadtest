#### 1. install：kind、kubectl、docker

#### 2. create kind cluster

kind create cluster --name kind
kubectl config current-context 

#### 3. install kwok 
```
KWOK_REPO=kubernetes-sigs/kwok
KWOK_REL=$(curl -s https://api.github.com/repos/$KWOK_REPO/releases/latest | jq -r .tag_name)

# kwok.yamlCRD defination + Controller Deployment: tell K8s:"there is Stage CR", deploy kwok to watch and execute.
kubectl apply -f "https://github.com/$KWOK_REPO/releases/download/$KWOK_REL/kwok.yaml"

# stage-fast.yaml: Stage(CR) rules: how to process specific Pod/Node?"
kubectl apply -f "https://github.com/${KWOK_REPO}/releases/download/${KWOK_LATEST_RELEASE}/stage-fast.yaml"
kubectl apply -f test/loadtest/kwok-timing-stages.yaml
```
kubectl apply -f <fast>/node-initialize.yaml
kubectl apply -f <fast>/node-heartbeat-with-lease.yaml
kubectl apply -f <fast>/pod-complete.yaml        # 可选,沙箱用不上

#### 4. create fake nodes
```
fake-node.yaml(先建 1 个验证,再 scale):

apiVersion: v1
kind: Node
metadata:
  annotations:
    kwok.x-k8s.io/node: fake          # ← kwok 接管这个节点
    node.alpha.kubernetes.io/ttl: "0"
  labels:
    type: kwok                        # ← 给 nodeSelector 用
  name: kwok-node-0
spec:
  taints:
    - key: kwok.x-k8s.io/node
      value: fake
      effect: NoSchedule              # ← 假节点默认 taint
status:
  allocatable: {cpu: "1000", memory: 1Ti, pods: "1000000"}
  capacity:    {cpu: "1000", memory: 1Ti, pods: "1000000"}
  conditions:
    - {type: Ready, status: "True", reason: KubeletReady}
  nodeInfo: {kubeletVersion: fake}
```


#### 5. 部署 OpenKruise Agents 组件(真 pod,跑在 kind 真节点上)

```
bash hack/generate-certificates.sh      # webhook 证书
make docker-build-controller docker-build-manager docker-build-runtime
kind load docker-image ... --name kind
make install-crd
make deploy-crd
make deploy-sandbox-manager

```



#### 6. create sandboxset

```
apiVersion: agents.kruise.io/v1alpha1
kind: SandboxSet          # ← 池子,不是单个 sandbox
metadata: {name: loadtest, namespace: default}
spec:
  replicas: 3             # ← 声明"要 3 个待领沙箱"
  template:
    spec:
      nodeSelector: {type: kwok}      # 钉到假节点
      tolerations: [{key: kwok.x-k8s.io/node, operator: Exists, effect: NoSchedule}]
      containers:
        - {name: sandbox, image: busybox:latest, resources: {requests: {cpu: 100m, memory: 64Mi}}}

kubectl apply -f /tmp/loadtest-sandboxset.yaml
```
#### 7. conduct loadtest













